services:
  - type: web
    name: toolhunterai
    runtime: python
    buildCommand: pip install -r requirements.txt gunicorn
    startCommand: gunicorn --bind 0.0.0.0:$PORT backend.web_app:app
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
